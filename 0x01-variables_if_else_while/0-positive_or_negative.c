/*Task 0 - By Sole*/
#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include <string.h>
/*
 * Main - start of execution
 */

int main(void)
{
int n;

srand(time(0))
rand() - RAND_MAX / 2;
if	(n > 0)
printf("%d is positive\n", n);
else if	(n < 0)
printf("%d is negative\n", n);
else
printf("%d is zero\n", n);
return (0);
}
